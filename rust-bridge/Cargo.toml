[package]
name = "overgraph_swift_bridge"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
overgraph = { path = "../Vendor/overgraph" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
